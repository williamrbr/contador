algoritmo "contador"
var
      contador, valor, salto:inteiro
inicio
      escreval("digite um numero que voce deseja que eu conte até ele:")
      leia(valor)
      escreva("Digite valor do salto: ")
      leia(salto)
		contador <- 0
		enquanto (contador <= valor) faca
		         escreva(contador)
			contador <- contador + salto
fimenquanto
		

fimalgoritmo
